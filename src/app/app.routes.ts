import { Routes } from '@angular/router';
import { DatosFormComponent } from './components/datos-form/datos-form.component';

export const routes: Routes = [
    { path: '', component:DatosFormComponent}
];
